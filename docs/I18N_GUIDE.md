# 国际化（i18n）开发指南

## 📋 概述

本项目已实现完整的国际化系统，支持三种语言：
- 🇮🇹 意大利语 (it) - 默认语言
- 🇬🇧 英语 (en)
- 🇪🇸 西班牙语 (es)

## 🚀 快速开始

### 在客户端组件中使用

```tsx
'use client';

import { useTranslation } from '@/hooks/useTranslation';

export default function MyComponent() {
  const { t, lang } = useTranslation();
  
  return (
    <div>
      <h1>{t('nav.home')}</h1>
      <p>Current language: {lang}</p>
    </div>
  );
}
```

### 在服务器组件中使用

```tsx
import { getTranslations } from '@/lib/i18n';

export default async function MyPage({ params }: { params: Promise<{ lang: string }> }) {
  const { lang } = await params;
  const t = getTranslations(lang);
  
  return <h1>{t('nav.home')}</h1>;
}
```

## 📁 文件结构

```
src/
├── locales/
│   ├── it.json      # 意大利语翻译
│   ├── en.json      # 英语翻译
│   └── es.json      # 西班牙语翻译
├── lib/
│   └── i18n.ts      # 翻译工具函数
└── hooks/
    └── useTranslation.ts  # React Hook
```

## 🔑 翻译键命名规范

翻译键使用点号分隔的嵌套结构：

```
nav.home                    → "HOME"
prodotti.title             → "Prodotti"
prodotti.pvInverter.title  → "PV Inverter"
garanzia.verificaGaranzia  → "Verifica Garanzia"
```

## ✍️ 添加新翻译

### 1. 在翻译文件中添加键值

编辑 `src/locales/it.json`、`src/locales/en.json`、`src/locales/es.json`：

```json
{
  "mySection": {
    "title": "我的标题",
    "description": "我的描述"
  }
}
```

### 2. 在代码中使用

```tsx
const { t } = useTranslation();
<h1>{t('mySection.title')}</h1>
<p>{t('mySection.description')}</p>
```

## 🔄 带参数的翻译

支持占位符替换：

```json
{
  "footer": {
    "copyright": "© {year} Afore Italia S.R.L. Tutti i diritti riservati."
  }
}
```

使用：

```tsx
{t('footer.copyright', { year: new Date().getFullYear().toString() })}
```

## ⚠️ 重要提醒

### ✅ 必须做的：

1. **所有用户可见的文本都必须使用翻译函数**
   - ❌ 错误：`<h1>Prodotti</h1>`
   - ✅ 正确：`<h1>{t('prodotti.title')}</h1>`

2. **添加新页面时，立即添加翻译**
   - 不要先写硬编码文本，再添加翻译
   - 直接在翻译文件中添加键值，然后使用翻译函数

3. **保持三种语言的翻译文件同步**
   - 添加新键时，确保在 `it.json`、`en.json`、`es.json` 中都添加

4. **使用有意义的键名**
   - ❌ 错误：`t('text1')`、`t('label')`
   - ✅ 正确：`t('prodotti.title')`、`t('nav.home')`

### ❌ 不要做的：

1. **不要硬编码任何文本**
   - 即使是按钮文本、标签、占位符等

2. **不要使用字符串拼接**
   - ❌ 错误：`"Prodotti " + category`
   - ✅ 正确：使用参数化翻译

3. **不要假设所有语言的结构相同**
   - 某些语言可能需要不同的语法结构

## 🐛 调试

如果翻译键不存在，系统会：
1. 在控制台输出警告
2. 返回键名本身（便于发现缺失的翻译）

## 📝 检查清单

在提交代码前，确保：

- [ ] 所有用户可见文本都使用了翻译函数
- [ ] 三种语言的翻译文件都已更新
- [ ] 没有硬编码的文本
- [ ] 翻译键命名清晰且有意义
- [ ] 测试了所有三种语言的显示效果

## 🔗 相关文件

- 翻译文件：`src/locales/*.json`
- 工具函数：`src/lib/i18n.ts`
- React Hook：`src/hooks/useTranslation.ts`

---

**记住：每次添加新功能时，都要考虑多语言支持！**

